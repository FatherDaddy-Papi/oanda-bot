name: RSI Bot

on:
  schedule:
    # Every hour at minute 5 — gives OANDA time to finalize the H1 candle that closed at :00.
    # GitHub cron is best-effort; actual execution may lag 5-15 min.
    - cron: '5 * * * *'
  workflow_dispatch:  # adds a "Run workflow" button in the Actions UI for manual triggers

# Don't kick off a second run if the previous one is still going (shouldn't happen but safety net).
concurrency:
  group: rsi-bot
  cancel-in-progress: false

jobs:
  trade:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    strategy:
      fail-fast: false  # if one instrument errors, the others still run
      matrix:
        # XAU_USD and XAG_USD removed 2026-05-26: realistic per-instrument
        # spreads (XAU ~81 pips, XAG ~348 pips) erase the edge (PF<1.0).
        # See compare_slippage.py for the before/after.
        instrument: [EUR_USD, NAS100_USD, GBP_JPY]
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install deps
        run: pip install --quiet oandapyV20 requests six python-dotenv

      - name: Run tick for ${{ matrix.instrument }}
        env:
          OANDA_ACCOUNT_ID: ${{ secrets.OANDA_ACCOUNT_ID }}
          OANDA_API_TOKEN: ${{ secrets.OANDA_API_TOKEN }}
          OANDA_ENV: practice
        run: python tick_once.py ${{ matrix.instrument }}
